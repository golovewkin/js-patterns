const MyCustomTree = {
    root = {
        children: [...],
    },

    iterator = {
        currentIndex = 0,
        getNext() {
            return
        },
        hasMore() {

        }
    }
}

MyCustomTree.getNext();
MyCustomTree.getNext();
MyCustomTree.iterator.hasMore();
