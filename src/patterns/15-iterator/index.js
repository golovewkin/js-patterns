export class Range {
  constructor(start, end) {
    this.start = start;
    this.end = end;
  }

  forEach (callback) {
    // todo: add implementation
  }

  getRange () {
    // todo: add implementation
  }
}

